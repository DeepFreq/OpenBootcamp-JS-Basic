// Bifurcation: if... else | It is a method of stock division. The script chooses one path or another depending on the actions that are performed.

let balance = 50;
let cash = 100;

if (cash < balance) { // --> Condition
    console.log("You can extract money") // --> The code is executed if it meets the condition
}
if (cash < balance) {
    console.log("You can extract money")
} else {
    console.log("Insufficient balance")
}